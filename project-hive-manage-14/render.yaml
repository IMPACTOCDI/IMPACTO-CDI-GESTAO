services:
  - type: web
    name: gestor-de-projetos-impacto-frontend
    env: static
    buildCommand: npm install --include=dev && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html 